================================================================================================================
⭐ TRABAJO INTEGRADOR FINAL: PROGRAMACIÓN I ⭐
================================================================================================================
INFORME TÉCNICO: SISTEMA DE GESTIÓN DE PAÍSES

📌 1. INTRODUCCIÓN AL PROYECTO

Este trabajo integrador final consiste en el desarrollo de una aplicación en Python que permite gestionar
información sobre países del mundo. El objetivo principal fue crear un programa que, a partir de una fuente
de datos externa (una API pública), descargue información real, la almacene de forma estructurada y ofrezca
al usuario herramientas para consultarla, filtrarla, ordenarla y analizarla de manera interactiva.

El proyecto no solo cumple con los requisitos técnicos, sino que también está diseñado pensando en la
claridad, la modularidad y la facilidad de uso. Se utilizó Python como lenguaje principal porque es ideal para
principiantes en programación, pero también es muy potente para trabajar con datos. Además, se incluyó el
uso de Docker para que cualquier persona pueda ejecutar el programa sin preocuparse por instalar Python o
las librerías necesarias.

A lo largo de este informe, se explicará paso a paso cómo está organizado el proyecto, qué hace cada parte
del código, cómo funciona el programa y por qué se tomaron ciertas decisiones técnicas. Todo está pensado
para que un estudiante de primer año de programación pueda entenderlo sin problemas, aunque también
incluye detalles más avanzados para quienes quieran profundizar.



📌 2. CONTEXTO Y MOTIVACIÓN

En la vida real, los programadores trabajan con datos que provienen de muchas fuentes: bases de datos,
archivos, sensores o, como en este caso, APIs (interfaces de programación de aplicaciones). Una API es como
una "puerta" que permite a un programa pedir información a otro sistema a través de internet.

En este proyecto, se utilizó la API pública REST Countries, que ofrece información actualizada sobre todos los
países del mundo: nombre, capital, población, área, moneda, idiomas, región, etc. Esta API es gratuita y no
requiere clave de acceso, lo que la hace perfecta para un proyecto educativo.

El programa no descarga los datos cada vez que se ejecuta. En cambio, los descarga una sola vez, los guarda
en un archivo CSV (un tipo de archivo que se puede abrir con Excel) y luego trabaja con ese archivo. Esto es
mucho más rápido y evita depender de la conexión a internet después de la primera ejecución.

📌 3. ESTRUCTURA GENERAL DEL PROYECTO

El proyecto está organizado en carpetas y archivos de forma clara y lógica. Esta organización es muy importante
en programación, porque permite encontrar rápidamente lo que se necesita y facilita el trabajo en equipo.

Árbol de directorios completo:
.
├── Trabajo_integrador_final/
│   ├── data/
│   │   └── paises.csv    <-- Archivo con los datos de los países (se genera automáticamente)
│   ├── src/
│   │   ├── funciones/   <-- Carpeta con todos los módulos de funcionalidades
│   │   │   ├── buscar.py   <-- Búsqueda de un país por nombre
│   │   │   ├── filtrar.py  <-- Filtros por región y población
│   │   │   ├── ordenar.py  <-- Ordenar por población o área
│   │   │   ├── estadisticas.py <-- Cálculo de estadísticas
│   │   │   └── menu.py     <-- Menú principal del programa
│   │   ├── getpaises.py  <-- Descarga datos de la API y crea el CSV
│   │   └── main.py       <-- Archivo principal que ejecuta todo
│   ├── Dockerfile       <-- Para ejecutar el programa en un contenedor
│   ├── README.md        <-- Instrucciones del proyecto
│   └── requirements.txt <-- Librerías necesarias

Explicación de cada parte:

data/: Es la carpeta donde se guarda el archivo paises.csv.

src/: Es la carpeta principal del código fuente ("source" en inglés).

src/funciones/: Contiene archivos con una funcionalidad específica (modularidad).

getpaises.py: Encargado de hablar con la API, descargar los datos y guardar en el CSV.

main.py: El "cerebro" del programa. Coordina el flujo de ejecución y el menú.

Dockerfile: Permite empaquetar el proyecto en un contenedor Docker.

README.md: Explica cómo instalar y usar el proyecto (la "portada" del repositorio).

requirements.txt: Contiene la lista de librerías externas que necesita el programa.

📌 4. DEPENDENCIAS DEL PROYECTO

El programa usa dos librerías externas que no vienen con Python por defecto:

✅ requests: Sirve para hacer peticiones HTTP (hablar con sitios web desde código). Se usa en getpaises.py para descargar los datos de la API.

✅ Rich: Se utiliza para crear texto enriquecido y de formato atractivo en la consola, permitiendo añadir color, estilo, tablas, barras de progreso, etc.

Estas librerías se instalan con un solo comando: pip install -r requirements.txt.

📌 5. FLUJO DE EJECUCIÓN DEL PROGRAMA

1.  El programa sigue un flujo claro y predecible:

2.  Se ejecuta main.py.

3.  El programa verifica si existe el archivo data/paises.csv.

4.  Si NO existe, ejecuta automáticamente getpaises.py para crearlo.

5.  Si SÍ existe, lo carga en memoria como una tabla (Lista de Diccionarios).

6.  Se muestra el menú principal.

7.  El usuario elige una opción (1 al 7).

9.  Según la opción, se llama a una función específica de la carpeta funciones/.

10. La función realiza su tarea y muestra resultados.

11. Se vuelve al menú hasta que el usuario elija "Salir".


📌 6. EXPLICACIÓN DETALLADA DE CADA MÓDULO

✅ 6.1 getpaises.py - Descarga de Datos desde la API

Función: obtener_paises().

Proceso: Se conecta a la API, recibe JSON, extrae info (nombre, capital, población, área, etc.), guarda en una lista de diccionarios, y convierte la tabla resultante a data/paises.csv.

Importancia: Crea la persistencia de datos. Maneja errores con try y except si no hay conexión o faltan datos.

✅ 6.2 main.py - El Archivo Principal (El "Director")

Función: Verifica el CSV, carga los datos (df), entra en un bucle infinito que muestra el menú y coordina la llamada a las funciones específicas.

Buena Práctica: Es el único punto de entrada (python src/main.py), manteniendo todo centralizado.

✅ 6.3 menu.py - La Interfaz con el Usuario

Función: mostrar_menu(). Imprime las 7 opciones, pide la entrada al usuario y devuelve la opción.

Propósito: Mantiene el código de la interfaz separado para facilitar el mantenimiento y el diseño.

✅ 6.4 buscar.py - Búsqueda de un País

Funcionalidad: Permite la búsqueda completa o parcial por nombre.

Lógica: Normaliza a minúsculas (.lower()) para búsquedas insensibles. Prioriza la coincidencia exacta (==) y luego busca coincidencias parciales (in) si el texto es de 3 o más caracteres (len(nombre_normalizado) >= 3).

Reporte: Presenta los resultados en una tabla formateada con Rich, mostrando Nombre, Población, Superficie y Continente, con números grandes formateados para legibilidad.

✅ 6.5 filtrar.py - Filtros Avanzados

filtrar_por_region(): Pide un continente o región (ej: "South America") y filtra los países por coincidencia de texto (insensible a mayúsculas/minúsculas). Implementa paginación (páginas de 10 elementos) para una mejor visualización.

filtrar_por_poblacion(): Pide un rango de población (mínima y máxima) y muestra países dentro de ese rango, incluyendo validación para evitar errores si el usuario ingresa texto.

✅ 6.6 ordenar.py - Ordenamiento de Datos

Funcionalidad: Ordena la lista de países según un criterio específico (Población o Área).

Conceptos: Utiliza la función integrada sorted() de Python junto con funciones anónimas lambda para indicar la clave de ordenamiento (ej. key=lambda fila: obtener_valor_seguro(fila, 'poblacion')). Muestra solo los 10 primeros resultados.

✅ 6.7 estadisticas.py - Análisis de Datos

Cálculos: Promedio de población y área, Mediana, Mínimo y Máximo, y Cantidad de países por región.

Uso: Emplea sum() para el promedio y la lógica de ordenamiento para encontrar la mediana, proporcionando una visión analítica de la información.

📌 7. USO CON DOCKER

Docker permite ejecutar el programa sin instalar Python ni las librerías en la máquina anfitriona.

Pasos:

Construir la imagen: docker build -t pais-app .

Ejecutar el contenedor: docker run -it pais-app
Dentro del contenedor, todo funciona igual. Esto es ideal para compartir el proyecto con compañeros o profesores.

📌 8. BUENAS PRÁCTICAS APLICADAS
           

EXPLICACIÓN         PRÁCTICA

Modularidad         Cada funcionalidad en un archivo separado.

Nombres Claros      buscar_pais, filtrar_por_region, etc.

Comentarios         Explican qué hace cada parte.

Manejo de Errores   try/except en entradas y conexiones.

Separación de Datos CSV en data/, código en src/.

Documentación       README claro y completo.

Reutilización       El CSV se genera una vez y se usa siempre.

📌 9. CONCLUSIÓN DEL TRABAJO

Este proyecto es un ejemplo de cómo puede construirse una aplicación en Python, desde la obtención de datos
hasta la interacción con el usuario. Aunque parece simple, incluye conceptos fundamentales de programación:

Uso de APIs

Manejo de archivos

Estructuras de datos (listas, diccionarios)

Modularidad

Control de flujo

Manejo de errores

Contenerización con Docker

Todo está explicado de forma clara, con una estructura organizada y comentarios útiles. El código es fácil de
leer, modificar y extender. Este trabajo no solo cumple con los objetivos del integrador, sino que sirve como
base para proyectos más avanzados.



==============================================================================================
🔧 10. ANEXO TÉCNICO: FUNDAMENTOS DE PROGRAMACIÓN APLICADOS
==============================================================================================
🔹 LISTAS

Definición: Una lista es una colección ordenada y mutable de elementos. Los elementos pueden ser de cualquier
tipo de dato (números, cadenas, o incluso otras listas y diccionarios). Permiten almacenar múltiples valores
bajo un solo nombre de variable.

Aplicación en el Proyecto:

Se utilizan para almacenar la colección principal de países después de ser cargados del archivo CSV.
Cada elemento de la lista es un diccionario que representa un país completo.

En la función de filtrar o ordenar, la lista de países se manipula (se reduce al aplicar un filtro o se
reestructura al ordenar) antes de ser presentada al usuario.

🔹 DICCIONARIOS

Definición: Un diccionario es una colección de elementos no ordenada, pero sí mutable, que almacena
datos en pares de clave-valor. Cada clave debe ser única e inmutable (generalmente una cadena o un
número), y se utiliza para acceder a su valor asociado.

Aplicación en el Proyecto:

Cada país dentro de la lista principal es un diccionario. Por ejemplo, {'Nombre': 'Argentina', 'Poblacion': 46735004, 'Continente': 'South America'}.

Las claves del diccionario (por ejemplo, 'Nombre', 'Poblacion') representan los campos de datos
(columnas), y los valores representan el dato específico de ese país (celdas). Esta estructura facilita el
acceso rápido y semántico a la información.

🔹 FUNCIONES

Definición: Una función es un bloque de código reusable que realiza una tarea específica. Ayudan a
organizar el código, hacerlo más legible (modularidad) y evitar la repetición de código.

Aplicación en el Proyecto:

La organización del código en archivos separados (buscar.py, filtrar.py, estadisticas.py) es un ejemplo
de modularidad basada en funciones.

Funciones como obtener_paises(), mostrar_menu(), ordenar_por_poblacion() o filtrar_por_region()
encapsulan la lógica necesaria para cada operación, haciendo que el archivo principal (main.py) actúe
únicamente como coordinador de llamadas.

🔹 CONDICIONALES (if, elif, else)

Definición: Las estructuras condicionales permiten al programa tomar decisiones y ejecutar diferentes
bloques de código basándose en si una condición booleana (Verdadero o Falso) es cumplida o no.

Aplicación en el Proyecto:

Manejo de Errores: Se usa try/except en getpaises.py para verificar la conexión a la API. Se usan
condicionales simples para verificar si un país existe antes de intentar leerlo, evitando que el programa
falle.

Flujo del Menú: En main.py, se utiliza una estructura if/elif/else para determinar qué función llamar
según la opción numérica (1 al 5) que el usuario ingrese en el menú.

Lógica de Búsqueda: En buscar.py, se usan condicionales para priorizar la coincidencia exacta
(if nombre_pais_normalizado == nombre_normalizado:) antes de pasar a la búsqueda parcial.

🔹 ORDENAMIENTOS

Definición: El ordenamiento es el proceso de organizar una colección de datos (como una lista de países)
en una secuencia específica (ascendente o descendente) basada en uno o más criterios (por ejemplo, población o área).

Aplicación en el Proyecto:

El módulo ordenar.py implementa el ordenamiento para mostrar los países más poblados o más extensos.

Se utiliza la función sorted() de Python, combinada con funciones anónimas (lambda), para indicar
la clave de ordenamiento. Esto permite ordenar una lista de diccionarios basándose en el valor de una
clave interna del diccionario (ej. 'poblacion'), en lugar de ordenar por el diccionario completo.

🔹 ESTADÍSTICAS BÁSICAS

Definición: Las estadísticas básicas son medidas cuantitativas utilizadas para resumir y describir un
conjunto de datos, como la media (promedio), la mediana, el valor mínimo, y el valor máximo.

Aplicación en el Proyecto:

El módulo estadisticas.py calcula y reporta el promedio (población media, área media) y la mediana (el
valor central) de los datos cargados.

Se usan funciones como sum() de Python para facilitar el cálculo del promedio, y la lógica de
ordenamiento (implícita para la mediana) para encontrar el valor central. Estos resultados ofrecen una
visión analítica de la información geográfica y demográfica.

🔹 ARCHIVOS CSV (Comma-Separated Values)

Definición: CSV es un formato de archivo de texto sin formato que se utiliza para almacenar datos
tabulares (hojas de cálculo o bases de datos). Cada línea del archivo representa una fila (un registro), y los
valores de las columnas están separados por un delimitador, comúnmente una coma.

Aplicación en el Proyecto:

El archivo data/paises.csv actúa como el almacén de datos persistente del proyecto.

El módulo getpaises.py es responsable de escribir los datos obtenidos de la API en este archivo.

Los demás módulos (main.py, buscar.py, etc.) son responsables de leer el archivo CSV en memoria al
inicio de la ejecución. Esto asegura que el programa no necesite conectarse a internet cada vez que se inicie.

